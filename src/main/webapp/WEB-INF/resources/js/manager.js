/**
 * 
 */
function go_find_id_form_mg() {
	$("#manager").attr("action", "find_id_form_mg").submit();
}

function go_find_pwd_form_mg() {
	$("#manager").attr("action", "find_pwd_form_mg").submit();
}